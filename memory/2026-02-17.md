# 2026-02-17

- Fixed OpenClaw model fallback reliability after OAuth cap issues:
  - Kept primary model as `openai-codex/gpt-5.3-codex`.
  - Configured fallback chain to API-key-backed models (`openai/gpt-4o-mini`, NVIDIA Kimi fallback).
  - Enabled `commands.restart=true` so gateway restarts can be triggered by config updates.
  - Verified config JSON validity and ran `openclaw doctor --non-interactive` successfully.

- User reported a previous bad config/corrupt JSON experience and asked to prevent recurrence.
  - Confirmed current `/home/michael/.openclaw/openclaw.json` is valid.
  - Stabilized fallback to known-working Kimi fallback path and avoided forcing Kimi as default.

- Backup/download context:
  - User asked to resume split file downloads but later decided to skip local download for now.
  - Cloud-side backup integrity was verified on VM:
    - `/home/mjcity0076/20260217-020828.bundle.tar.gz`
    - checksum validation result: `OK`
    - hash observed: `a303394bf903987a50a61d3b6ae8cadcfc7b5564e723f117a1e31cca9b9a222b`.

- Migration direction agreed:
  - User wants WordPress migration that includes moving to a newer stack.
  - Chosen approach: create a new VM (safe migration path), then restore/import and cut over.
  - User selected Debian 12 + 80GB boot disk while creating target VM in GCP console.
  - Work paused with user asked to click `Select` (boot disk dialog) and `Create`, then assistant continues with SSH/bootstrap/import.

## Afternoon Session

- User confirmed VM creation completed by saying "done" (x3).
- User explicitly requested model switch: "kimi 2.5".
- Confirmed active model is now `nvidia-nim/moonshotai/kimi-k2.5` (NVIDIA fallback path).
- Browser automation temporarily unavailable; waiting for SSH access to new VM.
- Migration plan remains: SSH into new VM → bootstrap stack → import backup → test → DNS cutover.

## Incident note — TechMyMoney DNS/availability (2026-02-17 late afternoon)
- Site restored and confirmed reachable after DNS rollback normalization.
- Key pitfall: rapid A-record toggles (`34.73.164.168` <-> `34.26.227.172`) created transient/stale client errors (`ERR_CONNECTION_CLOSED`) that looked like active outages.
- Durable lesson: verify authoritative NS state first, then public resolvers, then fresh-browser checks before deciding if outage persists.
- Operational discipline reinforced:
  - freeze DNS during diagnosis,
  - one change at a time,
  - confirm on all authoritative nameservers,
  - timestamp every action,
  - keep written incident log and recovery phases.
- Created durable troubleshooting checklist:
  - `/home/michael/.openclaw/workspace/techmymoney-troubleshooting-phases.md`
- Existing detailed change report retained at:
  - `/home/michael/.openclaw/workspace/techmymoney-change-log-2026-02-17.md`
  - `/home/michael/Documents/techmymoney-change-log-2026-02-17.md`
- Delivered exact upgrade execution checklist (commands + downtime estimate) in Word-compatible .doc format:
  - `/home/michael/.openclaw/workspace/techmymoney-upgrade-execution-checklist-2026-02-17.doc`
  - copied to `/home/michael/Documents/techmymoney-upgrade-execution-checklist-2026-02-17.doc`
- Emailed checklist to both recipients: `mjcity0076@gmail.com`, `michael@techmymoney.com` (Gmail confirmation: "Message sent").
